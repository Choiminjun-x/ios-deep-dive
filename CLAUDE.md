# 저장소 규칙

이 저장소는 Obsidian 볼트이면서 동시에 GitHub에서 읽히는 문서 저장소입니다.
두 곳에서 동일하게 렌더링되는 것이 최우선 제약입니다.

## 반드시 지킬 것

* 링크는 표준 마크다운만: `[텍스트](notes/foo.md)`. 위키링크 `[[ ]]` 사용 금지.
* 링크 경로는 링크를 담은 파일 기준 상대 경로. `/notes/foo.md` 같은 절대 경로 금지.
* `notes/` 파일명은 H1 제목을 그대로 쓴 한글, 확장자 `.md`. 그 외 폴더(`assets/` 등)의 파일명은 영문 kebab-case 유지.
* 파일명에 번호나 접두어를 붙이지 않음. 분류는 `README.md` 표에서 관리.
* 파일명에는 검색어가 될 단어를 포함.
* Callout은 GitHub alert 5종만: NOTE / TIP / IMPORTANT / WARNING / CAUTION.
* 문단 구분은 항상 빈 줄. 단일 개행에 의존하지 않음.
* 이미지는 `assets/`에 두고 상대 경로로 참조.

## 노트 포맷

* H1 다음 한 줄은 "이 노트가 답하는 질문". 문서 설명이 아니라 알고 싶었던 것.
* 여러 단계로 나뉘는 주제는 상단에 진행 상태 표.
* 각 섹션은 인용문(`>`) 한 줄 요약으로 시작.
* 각 섹션 끝에 `### 정리` — 결론만, 항목당 한 줄.
* 대화에서 다루지 않은 내용은 쓰지 않음.
* 확실하지 않은 것은 말미 "미확인 사항"에 별도 기록.

## 시각화

* 다이어그램은 SVG 파일로 만들어 `assets/`에 두고 이미지로 참조. Mermaid는 쓰지 않음.
* 경로는 `../assets/파일명.svg` (`notes/` 기준 상대 경로).
* 그림은 흐름, 표는 세부 정보. 같은 내용을 양쪽에 넣지 않음.

### SVG 스타일

* viewBox 680 고정, 배경 투명.
* 밝은 채우기 + 어두운 글자. 다크모드에서도 읽혀야 함.
* 회색이 기본. 컬러는 구분이 필요할 때만, 램프 2개 이내.
* 색으로 구분하면 상단에 한 줄 범례.
* 글자 2종: 제목 14px(500), 부제 12px(400). 부제는 5단어 이내.
* 박스 `rx=8`, 선 굵기 0.5, 화살표는 `#888780` 단일 색.

| 용도 | 채우기 | 테두리 | 제목 | 부제 |
|---|---|---|---|---|
| 회색 (기본) | `#F1EFE8` | `#5F5E5A` | `#2C2C2A` | `#5F5E5A` |
| 청록 (구분) | `#E1F5EE` | `#0F6E56` | `#04342C` | `#0F6E56` |

## 하지 말 것

* `notes/` 아래 노트 본문을 요청 없이 새로 작성하거나 재작성하지 않음.
  노트 본문 작성은 사용자가 직접 함. 요청 시 구조 제안·사실 검증·오탈자 수정만.
* 폴더를 미리 만들지 않음. 필요해질 때 사용자가 결정.
* `README.md`의 진행 상태 표는 행 추가/상태 변경만. 표 구조 임의 변경 금지.

## 커밋 전

`scripts/lint.sh`를 통과해야 합니다.